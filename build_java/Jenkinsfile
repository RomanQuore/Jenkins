pipeline {
    agent { label 'master' }
    //{ label 'master'}

        stages {
            
            stage ('installqqq') {
                steps {
                  dir("build_node"){
                        sh "npm install"
                  }
                   
                   
                }
            }
            
            stage ('install') {
                steps {
                  dir("build_java"){
                        echo "mvn clean compile test"
                  }
                   
                   
                }
            }

            stage ('test') {
                steps {
                    dir("build_java"){

                        echo "mvn -f pom.xml clean install -Dmaven.test.skip=true"
                        echo "tree"
                    }   
                }
            }
  
           
        }
}
